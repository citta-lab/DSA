/** 
 * Travelling in 2D grid. We start at top left corner and our goa is to travel to bottom right corner. 
 * we can only move "DOWN" or "RIGHT".
 * 
 * How many way we can travel to the bottom goal ? 
 * */

const gridTraveller = (matrix) => {

}

let grid = [
    [1,2,3],
    [5,6,7],
    [8,9,0]
];
console.log(gridTraveller(grid));